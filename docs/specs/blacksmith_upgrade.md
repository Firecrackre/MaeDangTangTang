# 대장 기술 업그레이드 기획서

> 로비 허브 진입점은 `lobby_hub.md` 참조. 판매가/구매가/상환 수식과 얽히므로 `pricing_calculation.md`를 함께 본다.
> 확률 보정이 큐브 결과에 미치는 부분은 `potential_determination.md`와 정합성을 유지한다.

---

## 1. 대장 기술 업그레이드

**대장 기술 업그레이드**는 플레이어가 게임 중 획득하는 강화 효과 시스템이다.

기존의 대장간 업그레이드 명칭은 사용하지 않고, 본 시스템의 명칭은 **대장 기술 업그레이드**로 통일한다.

대장 기술 업그레이드는 리그 오브 레전드 칼바람 아수라장 모드의 증강 시스템, TFT의 증강 시스템처럼 여러 개의 선택지 중 하나를 골라 적용하는 방식으로 구성한다.

---

## 2. 대장 기술 업그레이드 목표

대장 기술 업그레이드의 목표는 다음과 같다.

- 반복 플레이마다 다른 성장 방향 제공
- 플레이어가 현재 상황에 맞는 강화 효과를 선택하게 함
- 큐브, 무기 상점, 판매가, 상환금 등 여러 시스템에 변화를 줌
- 단순한 확률 게임이 아니라 전략적인 선택 요소를 추가함
- 같은 기술을 중복 획득했을 때 효과가 강화되도록 함

---

## 3. 대장 기술 획득 방식

대장 기술은 **대장 기술 업그레이드 UI에서 골드(메소)를 소모해 획득**할 수 있다.

대장 기술의 단계가 상승할수록, 필요 골드량도 상승한다.

메커톤 구현 기준 단계별 필요 골드량은 아래와 같다.

- 1단계: 7,500 메소
- 2단계: 12,500 메소
- 3단계: 20,000 메소

> ✔ **구현 완료** — 골드 구매형으로 전환됨. 로비 '대장 기술' 버튼 → `_AugmentManager:Open()` → 3택 창(상시 진입).
> 각 증강 하단의 구매 골드를 지불해 즉시 획득한다(`GameManager.RequestSelectAugment(key)`가 도달 단계 기준 비용을 검증·차감).
> 비용 = 도달할 단계 기준 **1단계 7,500 / 2단계 12,500 / 3단계 20,000**(`BlacksmithConfig.augmentCosts` → `GetAugmentCost(stage)`).
> 무료 3택은 폐지 — `RequestSettleDebt`에서 `RollAugmentChoices` 호출 제거, 대신 `ResetSession`에서 1회 롤 + 구매 성공 시 재추첨.

---

## 4. 대장 기술 선택 및 등장 방식

대장 기술 선택 흐름은 다음과 같다.

1. 플레이어가 대장 기술 업그레이드 메뉴 진입
2. 증강 등장 규칙에 따라 무작위 기술 선택지 3개 표시
3. 플레이어가 1개 선택 후 재화 지불
4. 선택한 기술 효과 적용
5. 로비로 복귀

선택지는 매번 무작위로 등장하며, 플레이어는 현재 상황에 가장 필요한 효과를 선택한다.

### 증강 등장 규칙

증강의 등장 규칙은 아래를 따른다.

1. 기본적으로 증강들은 동일한 등장 확률을 가진다. (4종이라면 각각 25%씩)
2. 만약 한 개의 증강이 업그레이드 최대 단계인 3단계에 도달 시, 더 이상 등장하지 않는다.
   - 2-1. 이런 경우, 남은 증강들이 동일한 등장 확률을 가진다.
     예를 들어, 1종류가 최대 업그레이드 단계에 도달했을 시, 나머지 3종의 대장 기술이 약 33.3%의 동일한 확률을 가지게 된다.
3. 만약 잔여 업그레이드 가능 증강이 3종 이하라면, UI 칸 수가 3개이므로, 모든 증강이 등장하게 된다.
   - 3-1. 잔여 업그레이드 가능 증강이 2종 또는 1종이라면, 남은 UI 칸은 빈 칸으로 둔다.
4. 플레이어가 한 개의 대장 기술을 업그레이드 완료하면, 창이 새로고침된다.
   - 4-1. 업그레이드를 하지 않고 UI를 퇴장 후 재진입 시 창이 새로고침되지 않고 그대로 남는다.

> ✔ **구현 완료** (`ui/AugmentGroup.ui` + `RootDesk/MyDesk/Augment/AugmentManager.mlua`) — 3택 슬롯머신 스핀 창.
> 잔여 ≤3종 전부 등장 + **잔여 2종 이하는 UI 칸 수 자체를 축소하고 가운데 정렬**(유저 확정: §4-3-1의 "빈 칸" 규칙 대체) /
> 구매 완료 시 창 새로고침(`RequestSelectAugment` 성공 → `RollAugmentChoices` 재추첨 → 클라 재스핀) /
> 미구매 퇴장 후 재진입 시 스핀 없이 유지(`lastChoicesData`/`lastOwnedData` 에지 감지). 전 증강 3단계 소진 시 "모든 대장 기술을 마스터했습니다" 안내.

---

## 5. 대장 기술 중복 획득

같은 종류의 대장 기술을 다시 획득할 수 있다.

동일한 기술을 중복 획득하면 효과가 누적되거나, 더 높은 수치로 강화된다.

### 예시

- 높은 등급 무기 등장 확률 증가 I
- 높은 등급 무기 등장 확률 증가 II
- 높은 등급 무기 등장 확률 증가 III

같은 기술을 여러 번 선택할수록 적용 수치가 증가한다.

중복 획득 방식은 다음 중 하나로 처리할 수 있다.

- 같은 기술의 수치를 단순 합산
- 같은 기술의 단계가 상승
- 기존 효과를 더 높은 수치로 교체

메커톤 버전에서는 관리가 쉬운 **단계 상승 방식**을 우선 사용한다.

또한, 메커톤 버전에 한정하여 한 종류당 3단계까지만 등장하고, 3단계에 달성하면 그 증강은 등장하지 않도록 한다.

---

## 6. 대장 기술 종류

대장 기술은 다음과 같은 종류로 구성한다.

### 더 좋은 물품 들여오기

무기 상점에 에픽, 유니크, 레전더리 등급 무기가 등장할 확률이 증가한다.

적용 대상:

- 무기 상점
- 무기 등급 등장 확률

효과 예시 (단계별 상점 등급 절대 확률표):

| 단계 | 커먼 | 레어 | 에픽 | 유니크 | 레전더리 |
|---|---|---|---|---|---|
| 0단계 (미보유) | 60% | 30% | 9% | 1% | 0% |
| 1단계 (소폭 증가) | 45% | 40% | 12% | 3% | 0% |
| 2단계 (증가) | 30% | 42% | 20% | 7% | 1% |
| 3단계 (크게 증가) | 18% | 38% | 27% | 13% | 4% |

> ✔ **구현 완료** — 위 단계별 절대 확률표가 그대로 적용된다 (`BlacksmithConfig.mlua` `betterGoodsGradeProb`·`weaponGrades.prob`(0단계) → `GetShopGradeProbTable`/`RollWeaponGradeDetail`, 배선 `RollShop`의 `GetAugmentStage("betterGoods")`). as-built 상세와 추첨 로그는 `weapon_shop.md` §9·§10 참조.

---

### 대장장이의 눈

현재 무기와 맞는 유효 옵션이 등장할 확률이 증가한다.

예를 들어 검을 사용 중이라면 STR, 공격력, 보스 공격력의 등장 확률이 증가한다.

적용 대상:

- 잠재능력 판정
- 옵션 등장 확률
- 유효 옵션 판정

효과 예시:

- 1단계: 유효 옵션 등장 확률 소폭 증가 (15%)
- 2단계: 유효 옵션 등장 확률 증가 (30%)
- 3단계: 유효 옵션 등장 확률 크게 증가 (45%)

> ✔ **구현 완료** — 15 / 30 / 45 (%) 적용됨 (`BlacksmithConfig.mlua` `augments.smithEye.stages`, `RequestUseCube` → `RollPotential`의 `validForceChance`).

---

### 가격 협상

무기 상점에서 무기를 구매할 때 필요한 메소가 감소한다.

적용 대상:

- 무기 상점
- 무기 구매가 계산

효과 예시:

- 1단계: 무기 구매가격 10% 감소
- 2단계: 무기 구매가격 18% 감소
- 3단계: 무기 구매가격 40% 감소

해당 효과는 게임이 끝날 때까지 유지된다.

> ✔ **구현 완료** — 10 / 18 / 40 (%) 적용됨 (`BlacksmithConfig.mlua` `augments.priceNego.stages`, 적용 `GetEffectiveBuyCost`, 1000단위 반올림).

---

### 위험 거래

상환 이자가 증가하는 대신 판매가도 증가한다.

리스크와 보상이 함께 커지는 고위험 고수익형 기술이다.

적용 대상:

- 빚 상환
- 판매가 계산

효과 예시:

- 1단계: 상환 이자 10% 증가, 판매가 10% 증가
- 2단계: 상환 이자 18% 증가, 판매가 18% 증가
- 3단계: 상환 이자 30% 증가, 판매가 30% 증가

플레이어는 상환 부담이 커지는 대신, 좋은 큐브 결과가 나왔을 때 더 큰 수익을 얻을 수 있다.

> ✔ **구현 완료** — 10 / 18 / 30 (%) 적용됨 (`BlacksmithConfig.mlua` `augments.riskyDeal.stages`, 판매가 적용 = `PricingCalculator:Calculate` 이후 `RequestUseCube`의 riskPct 가산, 이자 적용 = `ComputeRepaymentOf`).

---

### 귀한 태생

큐브를 사용했을 때 **더 좋은 등급(에픽·유니크·레전더리)의 잠재능력이 등장할 확률**이 증가한다.

큐브 스크래치(복권) 결과의 태생 등급 자체를 끌어올리는 기술로, 좋은 옵션이 붙기 전에 등급 배수부터 높여 준다.

적용 대상:

- 잠재능력 판정
- 잠재 등급 등장 확률

효과 예시 (단계별 잠재 등급 절대 확률표):

| 단계 | 커먼 | 레어 | 에픽 | 유니크 | 레전더리 |
|---|---|---|---|---|---|
| 0단계 (미보유) | 60% | 30% | 10% | 0% | 0% |
| 1단계 | 40% | 40% | 15% | 5% | 0% |
| 2단계 | 25% | 40% | 20% | 10% | 5% |
| 3단계 | 20% | 25% | 30% | 15% | 10% |

**트레이드오프 — 큐브 구매가 상승**: 이 기술의 단계가 오를수록 큐브 1개를 사는 가격도 함께 오른다(고위험 고수익).

| 단계 | 이상한 큐브 | 장인의 큐브 |
|---|---|---|
| 0단계 (미보유) | 7,000 | 45,000 |
| 1단계 | 8,000 | 48,000 |
| 2단계 | 9,000 | 55,000 |
| 3단계 | 10,000 | 80,000 |

> ✔ **구현 완료** — 위 단계별 절대 확률표가 그대로 적용된다 (`BlacksmithConfig.mlua` `cubeGradeProb`·`gradeProb`(0단계) → `GetCubeGradeProbTable`, 배선 `RollGrade`가 `GetAugmentStageOf("innateGrade")`를 사용). 큐브 가격은 `cubes.*.costs` + `GetCubeCost(cubeKey, stage)`가 SSOT(서버 `GetCubeCostOf` / 클라 `GetSyncedCubeCost`). 등급 배수는 `potential_determination.md` §4 참조.

---

### 장인의 집념

이전 큐브에서 무효 옵션이 2개 이상 등장한 경우, 다음 큐브의 첫 번째 옵션이 유효 옵션으로 등장할 확률이 증가한다.

좋지 않은 큐브 결과가 연속으로 이어지는 상황을 완화하고, 다음 큐브 사용에 대한 기대감을 높여주는 기술이다.

적용 대상:

* 잠재능력 판정
* 첫 번째 옵션의 유효 옵션 등장 확률

효과 예시:

* 1단계: 다음 큐브의 첫 번째 옵션 유효 확률 20%p 증가
* 2단계: 다음 큐브의 첫 번째 옵션 유효 확률 40%p 증가
* 3단계: 다음 큐브의 첫 번째 옵션이 반드시 유효 옵션으로 등장

이전 큐브에서 등장한 3개의 옵션 중 무효 옵션이 2개 이상이면 효과가 활성화된다.

활성화된 효과는 다음 큐브의 첫 번째 옵션에만 적용되며, 큐브를 사용한 이후 사라진다.

효과가 활성화된 상태에서 다시 발동 조건을 만족하더라도 효과는 중첩되지 않는다.

무기를 판매하거나 교체하더라도 활성화된 효과는 다음 큐브를 사용할 때까지 유지된다.

> 🛠 **구현 예정** — 이전 큐브의 무효 옵션 개수를 확인하여 효과를 활성화하고, 다음 `RollPotential` 실행 시 첫 번째 옵션의 유효 판정 확률을 보정한다.

---

### 재활용 전문가

큐브 사용 시 일정 확률로 큐브 사용 횟수가 차감되지 않는다.

한정된 큐브 사용 횟수를 보존하여 더 많은 잠재능력 재설정 기회를 얻을 수 있게 해주는 기술이다.

적용 대상:

* 큐브 사용 횟수
* 큐브 사용 횟수에 따른 상환금 증가

효과 예시:

* 1단계: 큐브 사용 시 10% 확률로 사용 횟수 차감 방지
* 2단계: 큐브 사용 시 20% 확률로 사용 횟수 차감 방지
* 3단계: 큐브 사용 시 35% 확률로 사용 횟수 차감 방지

효과 발동 여부는 큐브를 사용할 때마다 독립적으로 판정한다.

효과가 발동하더라도 잠재능력은 정상적으로 재설정된다.

효과가 발동한 경우 큐브 사용 횟수가 차감되지 않으며, 해당 큐브 사용으로 인한 상환금도 증가하지 않는다.

재활용 효과는 연속으로 발동할 수 있다.

> ✔ **구현 완료** — 10 / 20 / 35 (%) 적용됨 (`BlacksmithConfig.mlua` `augments.recycleExpert.stages`, 배선 `GameManager.DoUseCube`).
> 발동 시 큐브 사용 횟수 미증가 + 상환 트리거(5회 경계 판정) 스킵. **메소는 정상 차감**(적용 대상이 횟수·상환금뿐 — 유저 확정), 잠재는 정상 재설정.
> 발동 알림은 미러 property `RecycleProcCount` 증가 에지로 클라에 전달 → `HudManager`가 **시계 되감기 연출** 재생:
> 화면 전체 딤 + 좌상단 시계만 강조 → 바늘이 (n+1)→n 한 칸 되감김 + 모래시계 아이콘·말풍선("[재활용 전문가]\n큐브 차감 방지") 상승 + 효과음 →
> 되감기 완료 순간 화면 복귀(복권 상호작용 가능) → 약 2초 체류 후 하강. 연출 중 재발동 시 즉시 숨김 후 재등장.
> (§8 "확률 발동 비법은 발동 순간 강조 + 효과음/안내 문구" 요건은 이 연출이 충족 — 상시 보유 비법 아이콘 HUD는 별도 미구현.)

## 7. 대장 기술 적용 우선순위

대장 기술은 각 시스템의 계산 과정에 영향을 준다.

적용 우선순위는 다음과 같이 정리한다.

1. 상점 무기 생성 시 대장 기술 효과 확인
2. 무기 등급 등장 확률 보정
3. 무기 구매가 감소 효과 적용
4. 판매가 계산 시 판매가 증가 효과 적용
5. 빚 상환 시 상환금 증가 효과 적용

각 기술은 해당 시스템의 기본 계산 이후 또는 이전에 적용되는지 명확히 구분해야 한다.

---

## 8. 보유 비법 UI

플레이어가 현재 보유한 비법을 게임 화면에서 바로 확인할 수 있도록 보유 비법 UI를 제공한다.

보유 비법 UI는 게임 화면 왼쪽 위의 시계 아래쪽에 배치한다.

### 보유 비법 아이콘

플레이어가 획득한 비법은 각각 하나의 아이콘으로 표시한다.

* 획득하지 않은 비법은 표시하지 않는다.
* 같은 비법을 여러 번 획득한 경우 새로운 아이콘을 추가하지 않고 기존 아이콘의 단계가 상승한다.
* 각 아이콘에는 현재 비법 단계를 숫자로 표시한다.
* 비법의 최대 단계는 3단계이다.
* 비법 아이콘은 비법을 획득한 순서대로 배치한다.
* 보유 비법이 많아져 한 줄에 표시할 수 없는 경우 다음 줄에 이어서 표시한다.

---

### 비법 툴팁

플레이어가 비법 아이콘 위에 마우스를 올리면 해당 비법의 정보를 확인할 수 있는 툴팁이 표시된다.

툴팁에는 다음 정보가 포함된다.

* 비법 이름
* 현재 단계
* 비법 한 줄 설명
* 현재 단계에서 적용되는 효과

마우스가 비법 아이콘 영역에서 벗어나면 툴팁은 사라진다.

---

### 비법별 툴팁 설명

#### 더 좋은 물품 들여오기

무기 상점에 높은 등급의 무기가 등장할 확률이 증가한다.

현재 효과 예시:

* 1단계: 커먼 45% / 레어 40% / 에픽 12% / 유니크 3% / 레전더리 0%
* 2단계: 커먼 30% / 레어 42% / 에픽 20% / 유니크 7% / 레전더리 1%
* 3단계: 커먼 18% / 레어 38% / 에픽 27% / 유니크 13% / 레전더리 4%

---

#### 대장장이의 눈

현재 무기와 어울리는 유효 옵션이 등장할 확률이 증가한다.

현재 효과 예시:

* 1단계: 유효 옵션 등장 확률 15% 증가
* 2단계: 유효 옵션 등장 확률 30% 증가
* 3단계: 유효 옵션 등장 확률 45% 증가

---

#### 가격 협상

무기 상점에서 무기를 더 저렴한 가격에 구매할 수 있다.

현재 효과 예시:

* 1단계: 무기 구매가격 10% 감소
* 2단계: 무기 구매가격 18% 감소
* 3단계: 무기 구매가격 40% 감소

---

#### 위험 거래

상환 이자가 증가하는 대신 무기의 판매가도 함께 증가한다.

현재 효과 예시:

* 1단계: 상환 이자 10% 증가 / 판매가 10% 증가
* 2단계: 상환 이자 18% 증가 / 판매가 18% 증가
* 3단계: 상환 이자 30% 증가 / 판매가 30% 증가

---

#### 장인의 집념

이전 큐브에서 무효 옵션이 2개 이상 나왔다면 다음 큐브의 첫 번째 옵션 유효 확률이 증가한다.

현재 효과 예시:

* 1단계: 다음 큐브의 첫 번째 옵션 유효 확률 20%p 증가
* 2단계: 다음 큐브의 첫 번째 옵션 유효 확률 40%p 증가
* 3단계: 다음 큐브의 첫 번째 옵션이 반드시 유효 옵션으로 등장

---

#### 재활용 전문가

큐브 사용 시 일정 확률로 사용 횟수 차감을 방지한다.

현재 효과 예시:

* 1단계: 큐브 사용 시 10% 확률로 사용 횟수 차감 방지
* 2단계: 큐브 사용 시 20% 확률로 사용 횟수 차감 방지
* 3단계: 큐브 사용 시 35% 확률로 사용 횟수 차감 방지

---

### 비법 상태 표시

조건을 만족해야 발동하는 비법은 현재 발동 가능 여부를 아이콘으로 표시한다.

장인의 집념이 활성화된 경우 해당 아이콘에 테두리, 발광 또는 별도의 강조 효과를 표시한다.

장인의 집념 툴팁에는 현재 상태에 따라 다음 문구를 추가한다.

* 비활성화 상태: 발동 조건을 충족하지 않았습니다.
* 활성화 상태: 다음 큐브의 첫 번째 옵션에 효과가 적용됩니다.

재활용 전문가처럼 큐브 사용 시 확률적으로 발동하는 비법은 발동 순간 해당 아이콘을 잠시 강조한다.

플레이어가 비법 발동 여부를 명확하게 확인할 수 있도록 별도의 효과음 또는 안내 문구를 함께 출력한다.

> 🛠 **구현 예정** — 게임 화면 왼쪽 위 시계 아래에 보유 비법 아이콘을 표시한다. 아이콘에 마우스를 올리면 비법 이름, 현재 단계, 한 줄 설명 및 현재 단계의 적용 효과를 툴팁으로 출력한다.

